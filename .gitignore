downloads/
uploads/
sessions.json
# Zwischendatei des atomaren Schreibens (siehe saveSessions in server.js)
sessions.json.tmp
passwords_override.json
.env
node_modules/

# Datendateien — liegen nur auf dem Server, nicht im Repo
data/
*.migrated-backup
*.server-backup
posts.archive.*.json

# Zur Laufzeit vom Server geschrieben — NICHT tracken.
# Getrackt wuerden sie bei jedem Deploy von "git reset --hard" ueberschrieben
# (siehe .github/workflows/deploy.yml) und der Serverstand ginge verloren.
kursprogramm_kontext.json
redaktionsplan_meta.json

# Altlasten aus der Zeit vor dem data/-Verzeichnis — der Server nutzt data/
posts.json
performance.json
einreichungen.json

# Server-Backups und temporäre Dateien
server.js.backup_*
*.backup_*
vhs-pr-maschine-update.zip
users.json
FORCE_UPDATE.bat
update.ps1
update2.ps1
gitignore

# Temporäre Dateien aus Tests (16-07-2026)
checkup.sh
cleanup_demo_posts.js
setup_thema_clone.sh
tmp_thema/
tmp_users_thema.json
Session-Zusammenfassung-*.pdf

# Arbeitsordner aus der Umstellung auf die KI-Kennzeichnung (19.08.2026)
_backup_vor_ki/
_to_delete/
_ki_tmp/
_ki-update.zip
_neu_index.html
.fuse_hidden*
